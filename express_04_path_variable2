

const express=require('express');
const app=express();

app.get('/product/:pid/details/:pathName',(req, res)=>{
    const param=req.params;
    console.log(param);
    res.send("<h1>This is home page </h1>");
})

app.listen(3000,()=>console.log("server is running...."));
